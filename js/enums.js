const CarType = {
  NEW: "Brand New",
  USED: "Used",
};

const CarValueRange = {
  MIN: 10000,
  MAX: 200000,
  STEP: 1000,
};

const LeasePeriodRange = {
  MIN: 12,
  MAX: 60,
  STEP: 12,
};

const DownPaymentRange = {
  MIN: 10,
  MAX: 50,
  STEP: 5,
};

const InterestRate = {
  NEW: 2.99,
  USED: 3.7,
};
